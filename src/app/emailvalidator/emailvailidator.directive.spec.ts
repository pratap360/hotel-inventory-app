import { EmailvailidatorDirective } from './emailvailidator.directive';

describe('EmailvailidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailvailidatorDirective();
    expect(directive).toBeTruthy();
  });
});
